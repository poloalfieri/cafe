# ============================================================
# Frontend – Next.js  (cloná este archivo como .env)
# ============================================================

# ---------- Supabase (public – se exponen al browser) ----------
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# ---------- Supabase (server-only – NUNCA exponer) ----------
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# ---------- Mercado Pago ----------
NEXT_PUBLIC_MERCADO_PAGO_PUBLIC_KEY=TEST-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Para producción:
# NEXT_PUBLIC_MERCADO_PAGO_PUBLIC_KEY=APP_USR-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ---------- Backend Flask (server-only, usado por API Routes) ----------
# En desarrollo:
BACKEND_URL=http://localhost:5001
# En producción (Render / Railway / etc):
# BACKEND_URL=https://tu-backend.onrender.com

# ---------- Multi-tenant security ----------
# Clave compartida entre Next.js y Flask para validar el proxy interno.
# Generá una con: openssl rand -hex 32
INTERNAL_PROXY_KEY=dev-internal-proxy-key-change-in-production

# ---------- Database (Prisma – si se usa) ----------
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"

# ---------- Dev allowlist ----------
ALLOWED_DEV_EMAILS=dev1@example.com,dev2@example.com
